// Use shared Next.js configuration from @saw/config
const { createNextConfig } = require('../../packages/config/next.config')

// Export the config with any app-specific overrides
module.exports = createNextConfig({
  // Add app-specific configuration here if needed
})
